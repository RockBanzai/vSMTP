fn on_config(config) {

  config.server.logs.level = [ "info" ];

  config.server.interfaces = #{
      addr: ["0.0.0.0:25"],
  };

  config.app.rhai.filter_path = "/etc/vsmtp/filter.rhai";

  config
}
